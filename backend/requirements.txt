# requirements.txt

spacy>=3.7.0
transformers>=4.35.0
torch==2.2.2+cpu
sentence-transformers>=2.2.0
pandas>=2.0.0
numpy>=1.24.0
python-heideltime>=0.1.0
huggingface_hub>=0.20.0
python-dotenv>=1.0.0
requests>=2.31.0
tiktoken>=0.4.0
setuptools==68.2.2
fastapi>=0.104.1
uvicorn>=0.24.0
python-multipart>=0.0.20

# Note: AllenNLP is optional and has dependency conflicts with spaCy 3.x
# If you want to use AllenNLP SRL, install it separately in a virtual environment
# with compatible versions: pip install allennlp==2.9.3 allennlp-models==2.9.3
# allennlp>=2.10.0
# allennlp-models>=2.10.0

##################################################

# pip install -r requirements.txt
# pip install huggingface_hub[hf_xet]
# pip install bitsandbytes accelerate

# python download_models.py
# python -m backend.pipeline data/sample_story.txt
# python -m pip install google-genai 
# python -m pip install dotenv

# if SRL not working:
# delete step2a_srl_allennlp.py 
# in pipeline.py delete lines:
# 14, 23, 151-176

# if still not working, install packages based on errors